class Config:
    # For your current SIMULATION mode, interface is NOT actually used
    # but your code still requires a value to pass validation

    INTERFACE = "lo"   # loopback (safe default)

    DEBUG = True
